# Uploaded by Vagrant's salt provisioner to /etc/salt/minion on the compute node.
#
# The master address duplicates net:controller_ip from pillar. It has to: this
# file is read to reach the master, which is what serves pillar in the first
# place. Change both together.
master: 192.168.56.10

grains:
  role: compute
