# Uploaded by Vagrant's salt provisioner to /etc/salt/minion on the controller.
# The controller runs the master and is also its own minion, so it talks to
# itself over loopback rather than the lab network.
master: 127.0.0.1

grains:
  role: controller
