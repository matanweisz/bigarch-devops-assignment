# Local working material, not part of the submission
study/
bigarch-assignment.md

# Build outputs pulled from the builder VM
artifacts/

# Vagrant runtime state
.vagrant/

# Gateway test virtualenv (host-side only, never shipped in the image)
gateway/.venv/
__pycache__/
.pytest_cache/

# macOS noise
.DS_Store
