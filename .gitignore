# Local working material, not part of the submission
study/
bigarch-assignment.md

# Build outputs pulled from the builder VM
artifacts/

# Vagrant runtime state
.vagrant/

# Host-side Python test artifacts: the gateway venv, bytecode, pytest state.
# None of it is shipped in the container image.
gateway/.venv/
__pycache__/
.pytest_cache/

# macOS noise
.DS_Store
.superpowers/

# Generated locally by scripts/gen-secrets.sh, never committed
salt/pillar/secrets.sls

# Claude Code local dir (worktrees, settings)
.claude/
